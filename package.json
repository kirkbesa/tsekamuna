{
  "name": "tsekamuna",
  "version": "0.1.0",
  "description": "Browser extension nudge system for reducing misinformation sharing on Facebook among Filipino users.",
  "scripts": {
    "build": "node build.mjs && tailwindcss -i ./src/input.css -o ./content.css",
    "watch": "concurrently \"node build.mjs --watch\" \"tailwindcss -i ./src/input.css -o ./content.css --watch\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "concurrently": "^8.2.0",
    "esbuild": "^0.20.0",
    "postcss-import": "^16.0.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.4.0"
  }
}
